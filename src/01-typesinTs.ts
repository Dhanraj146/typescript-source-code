//type inferencing

let drink = "chai";
// drink = 25;

let cups = Math.random() > 0.5 ? 10 : "hello";

let channelNAme = "Chaiurcode";
// channelNAme = 12221; //Type 'number' is not assignable to type 'string'.

//type annotation -- all types in lower case

let chaiOrder: string = "CHAI ORDER";
chaiOrder = "masala chai order";

let check: boolean = true;
